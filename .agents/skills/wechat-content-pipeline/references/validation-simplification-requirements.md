# 校验简化需求（2026-07-22 用户确认）

用户正在逐步简化流水线校验逻辑。以下变更尚未执行代码修改，等用户明确说"修改方案"后再实施。

## 选题（discover）

只保留 3 项校验：

1. 类别必须在账号档案 `topic_discovery.categories` 内
2. 统一取 48 小时内热点（删除原 window_hours=24 + fallback_hours=72 双层窗口）
3. 7 天内（168h）选题不能与历史含义一致（删除原 30 天 + bigram 相似度阈值）

删除：
- 至少 2 个不同来源域名
- 每条证据必须有带时区发布时间
- 不能是未来时间
- 至少一条命中首选时间窗口
- 必须明确受影响人群
- 其他所有元数据格式检查

## 正文与来源（write / fact-check）

- `sources.md` 不记录、不校验、可删除
- 不要求来自两个不同域名

## 配图（illustrations）

- 生图失败不立即阻塞，重试一次
- 重试后再失败，允许无图继续
- 删除 image_count 数量校验（1-3 张）
- 删除 article.md 图片引用数量与 image_count 一致性检查

## 校验（validate）

- 删除预览生成
- 删除 leaf_count 记录
- 删除文件哈希记录
- 整个 validate 阶段删除

## 不变的部分

- humanize（去 AI 味）逻辑不变
- format（排版）逻辑不变
- cover（封面）逻辑不变
- draft（草稿）逻辑不变
